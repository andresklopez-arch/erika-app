npm run test-bluetooth-print
npm run test-decimal-discount
if [ -f .env.local ]; then
  npm run check-schema && npm run test-pos-matching && npm run test-quote-total
else
  echo "Aviso: .env.local no encontrado, se omiten check-schema/test-pos-matching/test-quote-total (requieren la base de datos real)."
fi
